akka.javasdk.dev-mode.http-port = 9101

# R217: the address a client connected from, which the pub/sub route uses as its identity when
# the client did not supply one. Off by default, and the identity is otherwise a fresh random
# string on every reconnection.
akka.http.server.remote-address-header = on
